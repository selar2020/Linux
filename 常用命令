cat  打印文件内容在屏幕上
cp   复制文件
mkdir  文件夹名称  新建文件夹
ls -l  列出文件详情
   -a  列出所有
   如 ls -al
rm   删除文件   
   格式  rm [选项]  文件名或文件夹名
    -f 即使文件只读也直接删除，不需确认
    -r 文件夹下所有文件一律删除
    -i 删除文件时一个个确认
    如  rm  -rf   abc
